- **`EnvDiagnosticBar` server component** Render masked status pills on the launch page for `DATABASE_URL`, `SCRAPER_USER_AGENT`, `CLAIMBOT_SUPPORT_EMAIL`, and `CLAIMBOT_SESSION_SECRET`; display only `configured`/`missing`, never values, and suppress the live-mode toggle until all pass.
- **`LaunchBlockerChecklist` card** Add a checklist component to the founder handoff page that maps each missing validator item to a row with the env key, its compliance purpose, and a server-driven status chip so operators see exactly what blocks launch.
- **`SecretSafeSnippet` code block** Provide a copyable `.env.local` template component containing the four missing keys with descriptive placeholders (e.g., `libsql://...`) so operators configure locally without the UI ever echoing real secrets.
- **`ShadowModeLock` in `ClaimStatusLockup`** Hardcode `isLive = false` and disable the live-filing switch when any critical env var is absent; render a non-dismissible Shadow config incomplete badge to prevent accidental live submissions.
- **`AuthGateBlock` server guard** Wrap the setup wizard so that if `CLAIMBOT_SESSION_SECRET` is missing, the server renders a blocking panel instead of intake steps, halting profile creation until the auth secret is set.
- **`ProofGateBanner` permanent UI** Mount a non-dismissible banner on `/goal` and the wizard stating that proof-required claims stay in review and category authorizations remain enforced, preventing operators from bypassing legal gates to fix blockers.
- **`CliCommandRow` copy blocks** Surface `npm run hosted:checklist` and `npm run validate:ui` as one-click copy `<pre>` components on the launch page so operators run exact validation commands without typing errors.
- **`BootstrapAuditStamp` footer** Add a server component that logs `shadow_mode=enforced`, `auth_gates=active`, and the missing env key list on startup, surfacing a tamper-evident footer note that the instance is running in safe mode.
